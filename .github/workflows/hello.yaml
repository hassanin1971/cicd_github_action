name: Hello Workflow
on:
    pull_request:
        branches:
            - main
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - name: Print Hello World
              run: echo "Hello, World!"
